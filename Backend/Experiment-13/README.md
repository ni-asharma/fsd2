MySQL database integration
Tested using Postman

## Setup

1. Clone repo
2. Install dependencies:
   pip install -r requirements.txt

3. Setup MySQL:
   Create database `student_db` and table `student`

4. Run:
   python app.py

## API Endpoints
POST /students
GET /students