Backend testing is done using Pytest and frontend testing using Vitest. 
GitHub Actions is used to implement CI, which automatically runs tests on every push. 
This ensures the code works correctly and helps detect errors early.
