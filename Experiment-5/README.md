In this experiment I have combined SPA routing with lazy loading to improve performance

1. Home, About and Contact components are loaded using React.lazy
2. I learnt that Suspense is used to display a loading message while components are fetched
3. Each page is loaded only when its route is accessed
   
